// Portal 2 agent: SourceAutoRecord + its TAS protocol server for the MCP controller.
plugin_load sar
sar_tas_protocol_server 6555
// Closed captions on screen (the agent reads them from screenshots).
closecaption 1
cc_subtitles 0
